{
  "name": "ForceAuditTrail",
  "version": "1.0.0",
  "description": "Makes the Salesforce SetupAuditTrail useful.",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest dist --silent=false --verbose --detectOpenHandles --coverage",
    "dev": "run-s build test",
    "watch": "run-p watch:build watch:test",
    "watch:build": "tsc --watch",
    "watch:test": "jest dist --silent=false --verbose --detectOpenHandles --coverage --watchAll"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^29.7.0"
  },
  "devDependencies": {
    "@babel/core": "^7.24.7",
    "@babel/preset-env": "^7.24.7",
    "@types/jest": "^29.2.6",
    "babel-jest": "^29.7.0",
    "npm-run-all": "^4.1.5",
    "parallelshell": "^3.0.2",
    "ts-jest": "^29.0.5"
  }
}
